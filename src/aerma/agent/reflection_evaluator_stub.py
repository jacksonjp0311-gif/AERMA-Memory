from typing import Dict, Any


class ReflectionEvaluatorStub:
    def evaluate(self) -> Dict[str, Any]:
        return {
            "reflection_evaluator": "stub",
            "gamma_rho_agent": None,
            "reflection_executed": False,
            "classification": "not_evaluated",
            "reason": "AERMA v1.2 includes stub only; full reflection evaluation is staged for a later version.",
        }
